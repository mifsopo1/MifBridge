
# (tools/blender-addon) — a .pyc was committed once already; it is build output
# Blender addon build output (tools/blender-addon/build_zip.py) and Blender's
# Python bytecode from the MCP layer (tools/mcp-server) and the Blender addon
# Release artifacts from tools/make_release.py - build output, never tracked.
# Run artefacts from tools/. Suite and sweep logs are regenerated by every run - 39 of them had
# SUBMITTED BY REPORTERS, and this is a public repository - it must not be committed by accident.
# The sweep interlock. A live runtime file naming the pid that currently owns the editor.
# Working files of the autonomous report loop. report_results.json in particular holds content
# accumulated untracked, one `git add tools/` away from being committed. The three .log files already
# already knows about, and removing them is a separate decision.
# and must never be tracked.
# rolling backups. The addon SOURCE under tools/blender-addon/MifBlender/ is
# run_all_suites.py writes a fresh log per invocation. suite_results.json IS tracked - it is the
# scratch dumps produced while auditing the MIF_BIND registry
# structured result the next run compares against - but the raw console logs are regenerated every
# time and only ever differ by timing noise.
# tracked (cooked_sweep_final, fuzz_final, fuzz_verify) stay tracked; git ignores this for files it
# tracked — only the packaged artifact and .blend backups are not.
*.bak
*.bak-*
*.blend1
*.blend2
*.pyc
*.sdf
*.sln
*.suo
.vs/
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Source/MifBridge/Private/bound_tmp.txt
__pycache__/
tools/*.log
tools/.sweep-lock
tools/audit_findings.jsonl
tools/audit_findings.pre-night-sweep.jsonl
tools/blender-addon/dist/
tools/dist/
tools/fuzz_run*.log
tools/report_queue.json
tools/report_queue.json
tools/report_results.json
tools/report_watch.log
tools/report_watch_state.json
tools/suite_run_*.log
